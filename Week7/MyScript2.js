// MyScript2 displays your name

var userName = "Daniel"; 

(function (dispName) {
    
    function displaytwo(dispName)
    {
        alert("The author's name is: " + dispName);
    }

    displaytwo(dispName);
})(userName);

//   2.  Using example in videos: build an object that contains information about your favorite movie

//   Enter the values into that object by creating an array of objects (at least two) and display the content (fields in the object) using a console.